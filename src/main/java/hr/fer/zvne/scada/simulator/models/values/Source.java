package hr.fer.zvne.scada.simulator.models.values;

public enum Source {
    APPLICATION,
    CALCULATED,
    MANUAL_ENTRY,
    NONSPECIFIC,
    PROC_MOD,
    REMOTE_SRC,
    REMOTE_SRC_INVALID,
    SCHEDULED
}
